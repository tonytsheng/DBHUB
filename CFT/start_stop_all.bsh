ACTIONIN=$1
ACTION=$(echo "$ACTIONIN" | tr '[:upper:]' '[:lower:]')-db-instance
#echo ${ACTION}

for ins in `aws rds describe-db-instances | jq ' .DBInstances[] | .DBInstanceIdentifier' | sed s/\"//g `
do
    echo $ins
    aws rds ${ACTION} --db-instance-identifier $ins
done

