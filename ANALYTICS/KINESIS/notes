ec2 instance - IAM role has Admin only

make sure the region is correct and the name of the stream is case sensitive



data streams            | firehose
-----------------------------------------------------------
low latency streaming   | put into s3, splunk, es, redshift
managed yet requires    |
  config for shards     |
storage for 7 days      | no data storage
real time               | near real time
need to write code for  | no need to write code
  sdk, agent, cw, KPL   
