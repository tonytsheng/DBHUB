# call like this
# DB_PW=`/home/ec2-user/DBHUB/BIN/getsecret.cli`
secret_id=$1

aws secretsmanager get-secret-value --profile ec2 --secret-id $secret_id | jq --raw-output '.SecretString' | jq -r .password
